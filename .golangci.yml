issues:
  exclude-use-default: false
linters:
  disable:
    - exhaustivestruct
    - goerr113
    - golint
    - interfacer
    - maligned
    - nlreturn
    - scopelint
    - varnamelen
    - wsl
  enable-all: true
linters-settings:
  wrapcheck:
    ignoreSigs:
      - .Blob(
      - .NewHTTPError(
run:
  concurrency: 4
  timeout: 30s
